name: Shell commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hopefully this works"
      - name: Check env version
        run: |
          node -v
          npm -v
